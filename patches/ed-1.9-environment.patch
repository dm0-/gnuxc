--- configure
+++ configure
@@ -22,10 +22,6 @@ datarootdir='$(prefix)/share'
 infodir='$(datarootdir)/info'
 mandir='$(datarootdir)/man'
 program_prefix=
-CC=gcc
-CPPFLAGS=
-CFLAGS='-Wall -W -O2'
-LDFLAGS=
 
 # checking whether we are using GNU C.
 ${CC} --version > /dev/null 2>&1
